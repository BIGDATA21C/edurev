import { ProjectsopPipe } from './projectsop.pipe';

describe('ProjectsopPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectsopPipe();
    expect(pipe).toBeTruthy();
  });
});
