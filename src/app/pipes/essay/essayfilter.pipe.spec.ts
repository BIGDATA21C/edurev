import { EssayfilterPipe } from './essayfilter.pipe';

describe('EssayfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EssayfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
