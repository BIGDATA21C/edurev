import { GeneralstudiesPipe } from './generalstudies.pipe';

describe('GeneralstudiesPipe', () => {
  it('create an instance', () => {
    const pipe = new GeneralstudiesPipe();
    expect(pipe).toBeTruthy();
  });
});
