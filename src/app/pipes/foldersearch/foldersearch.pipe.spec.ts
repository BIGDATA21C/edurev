import { FoldersearchPipe } from './foldersearch.pipe';

describe('FoldersearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FoldersearchPipe();
    expect(pipe).toBeTruthy();
  });
});
